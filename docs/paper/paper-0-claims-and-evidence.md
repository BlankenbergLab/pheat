# Claims And Evidence

| Claim | Required evidence | Current status | Remaining blocker |
| --- | --- | --- | --- |
| PHEAT supports schema-validated corpus specs. | Passing schema tests and examples. | Implemented for local/demo specs. | Review final paper-scale schema fields. |
| PHEAT builds user-defined reference manifests. | Offline build outputs and manifest validation. | Implemented for local fixtures. | Paper-scale archived snapshot build. |
| PHEAT records component/heterogen summaries. | CCD-like table tests and manifest rows. | Implemented with optional local table and demo fallback. | Use reviewed CCD export for paper corpora. |
| PHEAT benchmark outputs are machine-readable. | JSONL/CSV scripts and demo workflow. | Implemented scaffold. | Full comparator installation and archived runs. |
| PHEAT improves reproducibility of reference corpora. | Checksums, metadata, release/data DOIs. | Partial. | Real release DOI and dataset DOI. |
| PHEAT scoring is useful for native/decoy ranking. | Native/decoy benchmark metrics. | TODO. | Do not claim until real benchmark outputs exist. |
