# Scripts

Scripts write JSONL/CSV/JSON outputs for corpus and benchmark automation. They
are designed to run with `PYTHONPATH=../../src` from `benchmarks/paper-0`.

The scripts do not fabricate scientific scores. Missing optional comparators and
external backends are represented as explicit status or failure rows.
