# Corpus Specs

`user_defined_ids_demo.yml` is an offline fixture-backed demo spec. The
paper-scale specs live under `examples/corpora/` and should be copied or edited
with archived source snapshots before generating manuscript results.
